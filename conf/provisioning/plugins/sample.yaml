# # config file version
apiVersion: 1

# apps:
#   - type: grafana-example-app
#     org_name: Main Org.
#     disabled: true
#   - type: raintank-worldping-app
#     org_id: 1
#     jsonData:
#       apiKey: "API KEY"

# jpe - remove - seems like this is in gitignore?
apps:
  - type: 'grafana-llm-app'
    disabled: false
    jsonData:
      provider: anthropic
    secureJsonData:
      anthropicKey: $ANTHROPIC_API_KEY
